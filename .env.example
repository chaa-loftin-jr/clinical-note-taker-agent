# Copy to .env and fill in. Never commit the real .env file.

# Required if the Claude Code CLI bundled by claude-agent-sdk isn't already
# authenticated (e.g. via `claude login`).
ANTHROPIC_API_KEY=

# Optional overrides (see src/clinical_note_taker/config.py):
# CNT_ANTHROPIC_MODEL=claude-sonnet-5
# CNT_MAX_TURNS=8
# CNT_MAX_SUBMISSION_ATTEMPTS=2
# CNT_LOG_LEVEL=INFO
# CNT_REDACT_PII_IN_LOGS=true
# CNT_REQUIRE_CLINICIAN_REVIEW=true
